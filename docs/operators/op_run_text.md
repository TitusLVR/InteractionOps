# Run Text

Run the script currently open in the Text Editor from anywhere — for example with the mouse over the 3D Viewport — so you don't have to switch areas to test code.

**Hotkey:** Not bound by default — assign a key in *Preferences › iOps › Keymaps*, or run it from operator search.

## Tips

- A Text Editor must be open somewhere in the current workspace, otherwise nothing runs.
