# Open Asset in New Blender

Open the source .blend of a linked collection instance in a separate Blender window. Your current file stays untouched; edit the asset in the new window, save, and use *Reload Libraries* here to pick up the changes.

**Hotkey:** Not bound by default — select a linked collection instance and use *Open &lt;file&gt;.blend* in the iOps Edit pie.
